window.onload = draw;

  function draw(){
  var canvas = document.getElementById('canvaswiththehardER');

    if (canvas.getContext){
        var ctx = canvas.getContext('2d');
        // ctx.moveTo(200,500); //alguspunkt
        // ctx.lineTo(100,100); //lõpp-punkt
        // ctx.lineTo(100,150); //järgmine punkt
        // ctx.lineWidth = 2; //joone jämedus
        // ctx.strokeStyle = "black"; //joone värv
        // ctx.stroke(); //loome joone kahe punkti vahel

        //i ain't doing this
    }
    
  }